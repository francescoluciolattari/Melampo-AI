# Project Melampo research requirements
#
# Installs the editable package with development tools, clinical metadata
# support, semantic retrieval and visualization (the former `document`
# extra no longer exists: Docling was replaced by Nemotron-Parse).
# This profile is suitable for research notebooks, validation scaffolds and
# local RAG/document experiments without installing the full imaging/ML stack.
#
# Usage:
#   python -m pip install -r requirements-research.txt

-e .[dev,clinical,retrieval,viz]
