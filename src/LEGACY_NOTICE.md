# Legacy Notice

The original `src/README` and the old `src/models`, `src/training`, and `src/dreaming` prototype files are now legacy material.

The new baseline implementation lives under:

- `src/README.md`
- `src/melampo/`
- `src/resources/`

Use the new scaffold as the starting point for all future work.
