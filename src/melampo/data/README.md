# Data

This package contains data ingestion and normalization utilities for Melampo.

Current baseline includes:

- canonical case ingestion
- FHIR bundle normalization
- DICOM placeholder adapter
- minimal FHIR mapping

Future work should extend these modules toward PACS, RIS, EHR, and pathology interoperability.
